import type { BaseTranslation } from '../i18n-types';

const en = {
	schemas: {
		'no-content': {
			title: 'Define your content structure',
			body: 'A schema is like a form template — it sets the fields that every entry of that type will have. For example, a "Posts" schema might need a title, a body, and a publish date.',
		},
	},
} satisfies BaseTranslation;

export default en;
